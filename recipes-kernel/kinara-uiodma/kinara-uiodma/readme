To compile uiodma for ara device host run the below commands:

prerequisite:
make command and kernel headers should be installed on host

generate driver:
make clean
make

uiodma.ko will be generated
